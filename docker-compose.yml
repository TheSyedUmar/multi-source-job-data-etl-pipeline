version: "3.8"

services:
  localstack:
    image: localstack/localstack
    container_name: localstack-main
    ports:
      - "4566:4566"
    environment:
      - SERVICES=s3
      - DEBUG=1
    volumes:
      - "./localstack-data:/var/lib/localstack"